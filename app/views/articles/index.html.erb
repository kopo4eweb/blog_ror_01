<h1>Articles</h1>

<% @articles.each do |article| %>
    <div>
        <h3><%= article.title %></h3>
        <p><%= article.text %></p>
        <p><%= article.created_at %> | <%= link_to 'Show', article_path(article) %></p>
    </div>
    <hr>
<% end %>